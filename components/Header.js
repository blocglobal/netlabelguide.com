import React from 'react';
import Logo from './Logo';
import './Header.scss';

const Header = () => {
  return (
    <header className="Header">
      <Logo />
    </header>
  );
};

export default Header;
